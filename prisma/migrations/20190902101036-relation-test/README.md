# Migration `20190902101036-relation-test`

This migration has been generated by Ryan King at 9/2/2019, 10:10:36 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Account"("id" text NOT NULL  ,"user" text   REFERENCES "public"."User"("id") ON DELETE SET NULL,PRIMARY KEY ("id"))
;

CREATE UNIQUE INDEX "Account.id._UNIQUE" ON "public"."Account"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190821173925-role-migration-test..20190902101036-relation-test
--- datamodel.dml
+++ datamodel.dml
@@ -17,8 +17,9 @@
   email String  @unique
   name  String?
   posts Post[]
   role  Role?
+  account Account?
 }
 model Post {
   id        String   @default(cuid()) @id @unique
@@ -33,5 +34,9 @@
 model Role {
   id    String  @default(cuid()) @id @unique
   name  String?
   users User[]
+}
+
+model Account {
+  id    String  @default(cuid()) @id @unique
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190902101036-relation-test)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190902101036-relation-test'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
